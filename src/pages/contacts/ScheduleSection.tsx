import { ListItem, Stack, styled, Typography } from "@mui/material";

const Container = styled(Stack)({
  paddingInline: "10%",
  gap: "5%",
});

function ChurchSchedules() {
    <Container>
      <ListItem>
        <Typography variant="h5">ԿԻՐԱԿՆՕՐՅԱ ԾԱՌԱՅՈՒԹՅՈՒՆ</Typography>
      </ListItem>
    </Container>;
}
